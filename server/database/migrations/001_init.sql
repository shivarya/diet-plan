-- 001_init.sql — baseline schema.
-- The full baseline lives in database/schema.sql; importing that is equivalent
-- to running this migration on a fresh database. Future schema changes get
-- their own numbered, idempotent file here (002_..., 003_...).
SOURCE schema.sql;
